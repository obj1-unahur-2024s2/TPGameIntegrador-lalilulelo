
import wollok.game.*
import niveles.*

program TowerDefense {
  game.title("Tower Defense")
  game.width(10)
  game.height(10)
  game.cellSize(234)
 
  const nivel = new Nivel(
    
  )
  game.start()
  nivel.iniciar()
  
}