import cyclops.*
import wollok.game.*
import niveles.*
import src.monedas.*
import src.obstaculos.*
import src.puerta.*
import src.jugador.*
import src.menu.*

program TowerDefense {
  game.title("Grand Teft Cyclops")
  game.width(15)
  game.height(15)
  game.cellSize(234)
  game.start()
  nivel1.iniciar()
  jugador.controlesJugador()
}